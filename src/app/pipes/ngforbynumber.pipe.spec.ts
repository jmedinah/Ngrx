import { NgforbynumberPipe } from './ngforbynumber.pipe';

describe('NgforbynumberPipe', () => {
  it('create an instance', () => {
    const pipe = new NgforbynumberPipe();
    expect(pipe).toBeTruthy();
  });
});
